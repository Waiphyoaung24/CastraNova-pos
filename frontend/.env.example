# Copy to frontend/.env. Vite inlines VITE_* values into the client bundle,
# so never put a secret here — anything in this file is public.
VITE_API_URL=http://localhost:8000
MAILCATCHER_HOST=http://localhost:1080
